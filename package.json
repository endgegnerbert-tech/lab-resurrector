{
  "name": "flabs",
  "version": "1.0.0",
  "description": "flabs — physics playground. AI runs in your browser, not on the server.",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "scripts": {
    "start": "node server.js",
    "update-models": "node scripts/generate-model-catalog.js"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.3"
  }
}
